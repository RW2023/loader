import type { Viewport } from 'next';

export const viewport: Viewport = {
  themeColor: '#0a0a0a', // Dark mode background
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
};
